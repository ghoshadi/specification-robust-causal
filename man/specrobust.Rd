% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/specrobust.R
\name{specrobust}
\alias{specrobust}
\title{specrobust: Specification-Robust Causal Inference}
\usage{
specrobust(
  response,
  treatment,
  covariates,
  adj_sets,
  reg_mode = c("grf", "lm"),
  protect_vars = character(0),
  protect_fun = NULL,
  protect_orthonormalize = FALSE,
  alpha = 0.05,
  ref_index = 1,
  num_folds = 2,
  num_trees = 400,
  propensity_clip = 0.001,
  nu_regularize = 1e-06,
  bias_corr = TRUE,
  aipw_trim = 0,
  weight_trim = 0,
  n_boot = 1000,
  n_cores = 1,
  seed = 42,
  verbose = FALSE,
  return_full = FALSE
)
}
\arguments{
\item{response}{The outcomes.}

\item{treatment}{The binary treatment indicator, coded 0 and 1.}

\item{covariates}{A data frame or matrix of covariates with named columns.}

\item{adj_sets}{A list of at least two candidate adjustment sets, each a
character vector of column names of \code{covariates}. Their intersection must
be non-empty; the transfer weights are a function of that intersection.}

\item{reg_mode}{How the contrasts are estimated. \code{"grf"} uses cross-fitted
generalized random forests with augmented inverse probability weighting;
\code{"lm"} uses linear regression with treatment-covariate interactions and
bootstraps the standard errors.}

\item{protect_vars}{Character vector of variables in the intersection of
the adjustment sets whose means are held fixed by the reweighting. Only
available when \code{reg_mode = "grf"}.}

\item{protect_fun}{A function of the shared covariates returning a matrix
with one row per observation, whose column means are held fixed by the
reweighting. Only available when \code{reg_mode = "grf"}.}

\item{protect_orthonormalize}{Whether the protected block is orthonormalized
before the tilt is solved.}

\item{alpha}{Significance level, so the intervals have nominal coverage
\code{1 - alpha}.}

\item{ref_index}{Which candidate serves as the reference. A numeric index,
or one of \code{"first"}, \code{"largest"}, \code{"smallest"}.}

\item{num_folds}{Number of cross-fitting folds. \code{reg_mode = "grf"} only.}

\item{num_trees}{Number of trees in every forest, both layers.
\code{reg_mode = "grf"} only.}

\item{propensity_clip}{Propensity scores are clipped to
\verb{[propensity_clip, 1 - propensity_clip]}. \code{reg_mode = "grf"} only.}

\item{nu_regularize}{Ridge parameter of the system that combines the
candidates, shrinking towards equal weights. \code{reg_mode = "grf"} only.}

\item{bias_corr}{Whether the second-order bias correction is applied.
\code{reg_mode = "grf"} only.}

\item{aipw_trim}{Evaluation rows outside the \code{aipw_trim} and
\code{1 - aipw_trim} quantiles of any candidate pseudo-outcome are dropped.
\code{reg_mode = "grf"} only.}

\item{weight_trim}{Evaluation rows above the \code{1 - weight_trim} quantile of
the transfer weights are dropped. \code{reg_mode = "grf"} only.}

\item{n_boot}{Number of bootstrap replications, or \code{0} to fit the point
estimates and the transfer weights only, leaving \code{se} and \code{ci} as \code{NA}.
\code{reg_mode = "lm"} only.}

\item{n_cores}{Number of cores used for the bootstrap, by forking, so more
than one core has no effect on Windows. \code{reg_mode = "lm"} only.}

\item{seed}{Random seed for reproducibility.}

\item{verbose}{Whether progress information is printed while fitting.}

\item{return_full}{Whether the per-fold intermediates are attached to the
result as \verb{$full}. \code{reg_mode = "grf"} only.}
}
\value{
A \code{specrobust} object. The fields common to both modes are
\code{estimate}, \code{se} and \code{ci} for the reweighted population;
\code{candidate_estimates}, \code{candidate_se} and \code{candidate_ci} for the
individual adjustment sets; \code{hull_ci} for their convex hull; \code{weights}
for the transfer weights; and \code{adj_sets}, \code{common_covariates},
\code{covariates}, \code{alpha} and \code{n_used}. With \code{reg_mode = "grf"} it also
carries \code{nu}, the per-fold diagnostics, and, when something is protected,
\code{protected_summary}. With \code{reg_mode = "lm"} it also carries
\code{candidate_se_boot} and \code{candidate_ci_boot}.
}
\description{
Estimation and inference for the average treatment effect when several
candidate covariate adjustment sets are plausible and it is not known which
of them satisfies ignorability, as proposed by Ghosh and Rothenhaeusler
(2025+). The population is reweighted, as little as possible in
Kullback-Leibler divergence, so that all candidate estimands agree, and the
effect on that reweighted population is reported against the convex hull of
the per-candidate intervals.
}
\examples{
\donttest{
# Example 1, two confounders: {X1} is invalid, {X1, X2} is valid
set.seed(123)
n = 500
X1 = rnorm(n); X2 = rnorm(n)
A = as.numeric(runif(n) <= 1/(exp(5*X1 + 5*X2) + 1))
Y = A * (1 + X1 - 5*X2) + 4*X2 + rnorm(n)
out = specrobust(Y, A, data.frame(X1, X2), list("X1", c("X1", "X2")))
print(out)
plot(out)

# Example 2, M-bias: {X1} is valid, {X1, X2} is invalid
set.seed(123)
n = 500
U1 = rnorm(n); U2 = rnorm(n); X1 = rnorm(n)
A = as.numeric(U1 + X1 > 0)
X2 = U1 + U2
Y = ifelse(A == 1, 1 + X1 - U2 + rnorm(n), 5*U2 + rnorm(n))
out = specrobust(Y, A, data.frame(X1, X2), list("X1", c("X1", "X2")),
                 reg_mode = "lm", n_boot = 200)
print(out)
}

}
\references{
Ghosh, A., & Rothenhaeusler, D. (2025+).
Which Covariates to Adjust for? Specification-Robust Causal Inference in
Observational Studies.
}
